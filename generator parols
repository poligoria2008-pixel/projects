import secrets
import string

digits = string.digits
lowercase = string.ascii_lowercase
uppercase = string.ascii_uppercase
specials = '<>=/*-+@#$%^&()"№;%:?_'
all_chars = digits + lowercase + uppercase + specials

def generate_password(length, choice_type):
    if choice_type == 1:
        pool = lowercase
    elif choice_type == 2:
        pool = uppercase
    elif choice_type == 3:
        pool = digits
    elif choice_type == 4:
        pool = specials
    elif choice_type == 5:
        pool = all_chars
    else:
        raise ValueError("Неверный выбор типа символов")
    return ''.join(secrets.choice(pool) for _ in range(length))

print('ПРИВЕТ! Тебя приветствует программа, которая создаёт пароли.')
print('Вы можете сгенерировать в нашей программе любой пароль.')
print('Минимальная длина пароля — 8 символов. Допустимые значения: 8, 10, 15, 20.')

while True:
    try:
        length_input = input('Введите длину пароля: ')
        length = int(length_input)
        if length in (8, 10, 15, 20):
            break
        else:
            print('Пожалуйста, выберите одну из допустимых длин: 8, 10, 15 или 20.')
    except (ValueError, EOFError):
        print('Нужно ввести целое число.')

print('Из каких символов вы хотите составить свой пароль?')
print('1. Строчные буквы')
print('2. Заглавные буквы')
print('3. Цифры')
print('4. Специальные символы')
print('5. Всё вместе')

while True:
    try:
        choice_input = input('Введите число (1–5): ')
        choice_type = int(choice_input)
        if 1 <= choice_type <= 5:
            break
        else:
            print('Пожалуйста, введите число от 1 до 5.')
    except (ValueError, EOFError):
        print('Нужно ввести целое число.')

password = generate_password(length, choice_type)
print('Ваш пароль:', password)
